{% extends 'layout_front_login.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <h4>Регистрация / подтверждение</h4>

    {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}

    {% if user %}
        На Ваш Email - <strong>{{ user.email | raw }}</strong> отправлено письмо для подтверждения регистрации.
        Следуйте инструкциям в нём.
    {% else %}
        Неверные параметры.
    {% endif %}

{% endblock %}
